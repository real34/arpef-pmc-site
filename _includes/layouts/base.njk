<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="fr">

	{% include "components/head.njk" %}

	<body>

		<header>
			<em>
				<a href="{{ '/' | url }}">
					<img
						src="{{ '/static/img/hero.jpg' | url }}"
						class="hero"
						alt="{{ metadata.title }}"
						title="Agir, Représenter et Protéger les Enfants et leur Famille"
					>
				</a>
			</em>
			{% include "components/nav.njk" %}
		</header>

		<main>
			{{ layoutContent | safe }}
		</main>
	</body>

</html>
